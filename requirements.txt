# Perception
numpy>=1.26
pillow>=10.0

# The browser backend. After installing, fetch the browser itself:
#     playwright install chromium
playwright>=1.40

# Tests
pytest>=8.0

# Only needed for the DesktopBackend fallback:
# mss>=9.0
# pyautogui>=0.9.54
